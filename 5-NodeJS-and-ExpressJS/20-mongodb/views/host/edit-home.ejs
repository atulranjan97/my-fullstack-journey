<%- include('../partials/meta-tags') %>
</head>
<body class="min-h-screen bg-gradient-to-b from-gray-50 to-gray-100">
    <%- include('../partials/nav', {isActive: 'add-home'}) %>
    <main class="">
        <div class="max-w-md mx-auto bg-white rounded-lg shadow-md p-8 mt-8">
            <h1 class="text-3xl font-bold text-gray-800 mb-6 text-center"><%= editing ? 'Edit' : 'Add' %> your home</h1>
            <form action="/host/<%= editing ? 'edit-home' : 'add-home' %>" method="post" class="space-y-6">
                <div class="flex flex-col gap-y-2 ">
                    <input type="hidden" name="id" value="<%= editing ? home.id : '' %>">
                    <input type="text"
                        name="houseName"
                        value="<%= editing ? home.houseName : '' %>"
                        placeholder="Name of your house"
                        class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent"
                    />
                    <input type="number"
                        name="price"
                        value="<%= editing ? home.price : '' %>"
                        placeholder="Daily rent of your house"
                        class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent&::-webkit-outer-spin-button]:appearance-none [&::-webkit-inner-spin-button]:appearance-none"
                    />
                    <input type="text"
                        name="location"
                        value="<%= editing ? home.location : '' %>"
                        placeholder="Enter house location"
                        class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent"
                    />
                    <input type="text"
                        name="rating"
                        value="<%= editing ? home.rating : '' %>"
                        placeholder="Rating of the house"
                        class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent"
                    />
                    <input type="text"
                        name="photoUrl"
                        value="<%= editing ? home.photoUrl : '' %>"
                        placeholder="Upload a photo"
                        class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent"
                    />
                    <textarea
                        name="description"
                        placeholder="Describe your house"
                        class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent"
                        rows="4"
                    ><%= editing ? home.description : '' %></textarea>
                </div>
                <div class="flex justify-center">
                    <input type="submit" 
                        value="<%= editing ? 'Update' : 'Add' %> Home"
                        class="px-6 py-3 bg-sky-600 hover:bg-sky-700 text-white font-semibold rounded-lg transition-colors duration-200 cursor-pointer"
                    >
                </div>
            </form>
        </div>
    </main>
</body>
</html>

<!-- styling it with tailwind classes with the help of `CursorAI` -->